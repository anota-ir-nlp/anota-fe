// i18n.config.ts
export default defineI18nConfig(() => ({
  legacy: false,
  locale: "id",
  messages: {
    id: {
      // You can add your application-specific translations here if needed
      // For now, an empty object for messages is fine to provide the context
    },
  },
}));
